{
  "name": "poc-plpgsql-analyzer-ts-test",
  "scripts": {
    "test": "sh -c 'wasm-pack build --dev --target nodejs ../.. && npm run vitest'",
    "vitest": "vitest"
  },
  "devDependencies": {
    "poc-plpgsql-analyzer": "file:../../pkg",
    "prettier": "^2.8.8",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
